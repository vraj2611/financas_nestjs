import { describe, expect, test, afterAll } from '@jest/globals';



describe('Model Project', () => {
    beforeAll(async () => {
        
    })

    beforeEach(async () => {
    
    });

    test('be defined', () => {
    
    });
});